# Running sglang on DGX-A100

## 1. Install `uv` and `pixi`

## 2. Run serve
```bash
bash serve.sh
```

## 3. (Optional) deploy user unit to automatically start/stop inference at designated time
```bash
bash services/deploy.sh
```
